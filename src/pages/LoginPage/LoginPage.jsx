import LoginForm from '../../components/LoginForm/LoginForm';
import Section from '../../components/ui/Section';

const LoginPage = () => {
  return (
    <Section sx={{ paddingTop: '75px' }}>
      <LoginForm />
    </Section>
  );
};

export default LoginPage;
